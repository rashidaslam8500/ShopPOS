<UserControl x:Class="ShopPOS.WPF.Views.AuditLogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel>
        <DockPanel DockPanel.Dock="Top" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="Audit Trail" FontSize="24" FontWeight="Bold"/>
                <TextBlock Text="Every sale, edit, return, and login is logged here. Select entries to clean up with owner authorization."
                           Foreground="{StaticResource TextMutedBrush}" TextWrapping="Wrap"/>
            </StackPanel>
            <Button Content="Refresh" DockPanel.Dock="Right" HorizontalAlignment="Right" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding LoadCommand}"/>
        </DockPanel>

        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Margin="0,16,0,0">
            <Button Content="Delete Selected" Style="{StaticResource NavButtonStyle}" Margin="0,0,12,0"
                    Command="{Binding DeleteSelectedCommand}"/>
            <Button Content="Refresh" Style="{StaticResource NavButtonStyle}" Command="{Binding LoadCommand}"/>
        </StackPanel>

        <DataGrid ItemsSource="{Binding Logs}" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTemplateColumn Width="Auto" MinWidth="50">
                    <DataGridTemplateColumn.Header>
                        <CheckBox IsChecked="{Binding DataContext.SelectAll, RelativeSource={RelativeSource AncestorType=DataGrid}, Mode=TwoWay}"/>
                    </DataGridTemplateColumn.Header>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Time (UTC)" Binding="{Binding Timestamp, StringFormat=g}" Width="Auto" MinWidth="140"/>
                <DataGridTextColumn Header="User" Binding="{Binding Username}" Width="Auto" MinWidth="90"/>
                <DataGridTextColumn Header="Action" Binding="{Binding Action}" Width="Auto" MinWidth="110"/>
                <DataGridTextColumn Header="Entity" Binding="{Binding EntityType}" Width="Auto" MinWidth="80"/>
                <DataGridTextColumn Header="Details" Binding="{Binding Details}" Width="Auto" MinWidth="160"/>
                <DataGridTemplateColumn Header="" Width="*" MinWidth="90">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Delete"
                                    Style="{StaticResource NavButtonStyle}"
                                    Padding="8,4"
                                    Command="{Binding DataContext.DeleteSingleCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
